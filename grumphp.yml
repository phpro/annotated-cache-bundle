parameters:
    git_dir: .
    bin_dir: ./vendor/bin
    tasks:
        phpcs:
            standard: PSR2
            ignore_patterns:
                - tests/*.php
        phpunit: ~
        composer:
            no_check_lock: true
